# Python (backend)
backend/.venv/
__pycache__/
*.pyc
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Env
.env
deploy/velociraptor/.env

# Velociraptor 實際憑證/設定/資料(範本除外,見 deploy/velociraptor/README.md)
# etc/ 內是 docker compose 首次啟動自動產生的 server.config.yaml(含私鑰/憑證),
# datastore/ 是端點資料與 datastore,兩者都不應進版控。
deploy/velociraptor/etc/
deploy/velociraptor/datastore/
deploy/velociraptor/*.yaml
!deploy/velociraptor/*.example.yaml

# SNMP 監控目標清單(可能含內部 IP 配置細節,範本除外)
deploy/snmp/*.json
!deploy/snmp/*.example.json

# ASUS exporter 監控目標清單(可能含內部 IP 配置細節,範本除外)
deploy/asus_exporter/*.json
!deploy/asus_exporter/*.example.json

# ASUS 路由器管理介面帳密(每台一份,範本除外)
deploy/asus-exporter/router-*.env

# OS
.DS_Store
Thumbs.db
