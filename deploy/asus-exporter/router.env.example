# 複製成 router-1.env / router-2.env / router-3.env(一台路由器一份),
# 填入該台的管理介面帳密與 IP。這是路由器本機管理帳號,不是 mini-edr
# 帳號,也不是 ASUS 雲端帳號。
ASUS_USERNAME=admin
ASUS_PASSWORD=change-me
# exporter 只能從路由器的 WAN 端存取(exporter/NAS 跟路由器的 LAN 端不是
# 同一個子網),所以這裡要填「Enable Web Access from WAN」開啟後看到的
# WAN 端管理 IP,不是連上該路由器 WiFi 後看到的 LAN 閘道 IP。
ASUS_IP=192.168.2.149

# 以下兩個有預設值(https / 8443,見 asus-exporter.py 開頭的差異說明),
# 通常不用改——ASUS 韌體開啟「Enable Web Access from WAN」後預設就是
# HTTPS + 8443。如果你的路由器被改成別的 port,再覆寫這裡。
# ASUS_SCHEME=https
# ASUS_PORT=8443
