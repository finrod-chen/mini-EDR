# backend 位置。前後端跨 origin(Vite dev server 5173 / backend 8000),
# 登入用的 session cookie 靠 backend 的 CORS allow_credentials 設定跨網域帶,
# 不是靠同源代理(見 backend .env.example 的 FRONTEND_ORIGIN 說明)。
VITE_API_BASE_URL=http://localhost:8000
